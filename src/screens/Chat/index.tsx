import React from "react";
import { View } from "react-native";
import { Text } from "../../design";

export const ChatScreen = ({ navigation }: { navigation: any }) => {
  return (
    <View>
      <Text value={"Comming Sooon"} h1 />
    </View>
  );
};
